import TodoList from '../components/TodoList';

const HomePage = () => {
  return (
    <section className="max-w-2xl md:max-w-3xl mx-auto">
      <TodoList />
    </section>
  );
};

export default HomePage;
